<h1>ユーザ一覧</h1>

<%= search_form_for @q, url: users_path, method: :get do |f| %>
  <%= f.label :name_cont, "名前検索" %>
  <%= f.search_field :name_cont %>
  <%= f.submit "検索" %>
<% end %>

<ul>
  <% @users.each do |user| %>
    <li><%= user.name %> (<%= user.email %>)</li>
  <% end %>
</ul>
